export function isFitObject<T, N>(el: T, params: N): boolean {
    let isTrue = true

    for (let key in params) {
        // let value = el[key]
        // console.log(value)
    }

    return false
}